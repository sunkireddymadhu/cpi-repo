{
  "name": "cpi-pipeline",
  "version": "1.0.1",
  "scripts": {
    "build": "echo \"🔍 Validating CPI Artifacts...\" && FILE=$(git diff --name-only HEAD~1 HEAD | grep '.zip') && echo \"Processing $FILE\" && unzip -t \"$FILE\" && mkdir -p build && cp \"$FILE\" build/ && echo \"✅ Done\""
  }
}
