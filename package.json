{
  "name": "cpi-pipeline",
  "version": "1.0.1",
  "scripts": {
    "build": "echo \"🔍 Validating CPI Artifacts...\" && FILE=$(find iflows -name '*.zip' | tail -1) && echo \"Processing $FILE\" && unzip -t \"$FILE\" && mkdir -p build && cp \"$FILE\" build/ && echo \"✅ Done\""
  }
}
